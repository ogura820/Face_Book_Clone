<!DOCTYPE html>
<html>
  <head>
    <title>Yuzintyo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <h1>Face Book風アプリ</h1>
      <% if logged_in? %>
      <ul class="top_menu clearfix"><ul class="top_menu clearfix">
        <li><a><%= link_to "Profile", user_path(current_user.id) %></a></li>
        <li><a><%= link_to "Logout", session_path(current_user.id), method: :delete %></a></li>
        <li><a><%= link_to "投稿一覧画面", pictures_path %></a></li>
      <% else %>
      <ul class="top_menu clearfix"><ul class="top_menu clearfix">
        <li><a><%= link_to "Sign up", new_user_path %></a></li>
        <li><a><%= link_to "Login", new_session_path %></a></li>
      <% end %>
    </header>
    <% flash.each do |key, value| %>
      <%= content_tag(:div, value, class: "#{key}") %>
    <% end %>
    <%= yield %>
    <footer>
     <p>Copyright &copy; 2023 OnO.</p>
    </footer>
  
  </body>
</html>
